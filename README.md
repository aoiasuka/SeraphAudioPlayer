# Seraph Audio Player

Premium local HiFi audio player built with Rust, Tauri, and React.

Seraph Audio Player 是一款面向本地高保真音乐播放的桌面播放器，目标是在 Windows 上提供低延迟、可控、稳定的本地音频播放体验。底层播放、设备与切歌状态由 Rust 后端负责，React 前端只作为 UI 投影层。

## 核心特性

- **Rust 音频后端**：播放状态、切歌、结束续播、上一首/下一首由 Rust 统一管理，减少前后端状态分叉。
- **WASAPI Exclusive**：支持 Windows WASAPI 独占输出，绕过系统混音路径。
- **多格式解码**：基于 Symphonia / FFmpeg 的多级解码路径，支持常见本地音频与部分流媒体缓存文件。
- **DSD/高采样率处理**：包含 DSD PCM 转换与重采样处理模块。
- **参数均衡器（EQ）+ Crossfeed**：侧栏「系统 → EQ 均衡器」提供细粒度参数 EQ（RBJ biquad，峰化/搁架/低高通多类型、逐段频率·增益·Q 可调）、预放大、耳机 crossfeed；内置 10 种曲风预设，支持 AutoEq / EqualizerAPO 预设导入与 JSON/APO 导出、用户预设保存；可选是否对 DSD 生效。DSP 运行在解码线程、播放中热更新、seek 自动重置滤波状态。
- **系统媒体控件（SMTC）**：键盘/蓝牙媒体键控制播放，系统音量浮窗与锁屏展示曲目标题、艺术家、封面与进度；可在设置 → 系统集成中停用。
- **任务栏集成**：悬停任务栏图标的缩略图窗口内提供上一首/播放暂停/下一首按钮（图标随任务栏深浅主题自适应），任务栏图标叠加播放进度（暂停转黄色）；可选「任务栏歌词条」——贴在任务栏上的档案纸签浮层，实时单行歌词 + 旋转封面 + 底边进度线，悬停出播控按钮，可拖拽记忆位置，跟随任务栏变化自动吸附、全屏应用与自动隐藏任务栏收起时自动避让、深色任务栏自动切换墨签配色，支持鼠标穿透的「仅显示模式」。均在设置 → 系统集成开关。
- **实时频谱可视化**：渲染线程实时安全旁路输出样本，FFT log 频段柱状频谱（48 段）随播放展示。
- **声学分析套件**：侧栏「系统 → 声学分析」提供六仪表实时分析页——响度计（EBU R128 / ITU BS.1770-4 语义：M/S/I、LRA、真峰≈、目标偏差标尺）、电平表（PEAK/RMS 分行条表与模拟 VU 表盘双模式，300ms 表针弹道、0 VU = -18 dBFS）、立体声声场仪（极坐标/李萨如散点 + 相关度表）、96 频点对数频谱（样条平滑迹线、峰值保持、悬停游标读数）、频谱瀑布（墨线山脊 / 纸墨热图双模式）、示波器（时间域波形，零交叉触发锁相、L/R 叠加或分离）；面板与显示内容可在「PANELS 面板设置」中自定义并持久化，且支持「LAYOUT 布局」图纸编辑模式——在 12×12 网格上拖拽移动、角部缩放六个仪表，自由定义大小与位置（自动布局仍为默认，隐藏面板自动补位）；全部由播放输出的真实样本驱动，渲染节流 + 离屏增量绘制控制开销。
- **本地封面识别**：导入时提取音频内嵌封面（按内容哈希本地缓存、同专辑去重），经 Tauri asset 协议在播放条转盘、专辑/艺术家视图中展示；旧曲库首次启动自动补扫，孤儿封面自动清理；无内嵌封面的曲目支持在线匹配（QQ 音乐 / iTunes）。
- **曲库检索与排序**：曲目列表支持标题/艺术家/专辑检索与多键排序，虚拟列表大曲库流畅滚动。
- **全局右键菜单**：曲目行、歌单、专辑/艺术家卡片、播放条、UP NEXT、歌词稿均有场景化右键菜单（播放/收藏/加入歌单/曲目信息/复制/在资源管理器中定位/删除等）；文本输入框保留系统编辑菜单。
- **歌单管理**：加入歌单、歌单内排序、移除、重命名，支持 M3U8 清单导入导出。
- **播放进度恢复**：重启后恢复上次曲目与播放位置，一键续播；可在设置 → 系统集成中通过「记忆播放」开关关闭。
- **配置一键导出 / 导入**：设置 → 系统集成提供「配置备份」，播放偏好、EQ 与 DSP、声学分析设置打包为单个 JSON 导出；导入校验后自动重载生效，不覆盖收藏、歌单等本机个人数据。
- **应用内检查更新**：启动静默检查 GitHub Releases，发现新版可一键前往下载页。
- **Bilibili 音频导入与缓存**：支持导入 Bilibili 音频并管理本地缓存。
- **缓存保护机制**：缓存目录写入 `.seraph-cache` 标记，清理时只处理受管理的缓存文件，降低误删风险。
- **状态一致性保护**：播放命令会等待 Rust 音频线程返回真实执行结果，再同步给前端 UI。
- **持久化迁移**：前端播放偏好使用版本化持久化状态，旧字段会在启动时自动迁移到当前结构。
- **收窄 Tauri 权限**：桌面壳只开启窗口控制、事件监听、拖放和打开/保存文件对话框所需权限。
- **中英文 Windows 安装包**：Tauri 打包配置会生成英文/中文 MSI，并为 NSIS EXE 安装器启用语言选择。

## 架构概览

```text
Seraph Audio Player
├─ crates/
│  ├─ seraph-core/        # 共享事件、状态与领域类型
│  ├─ seraph-audio/       # 播放控制器、WASAPI/CPAL 输出、播放会话
│  ├─ seraph-decoder/     # Symphonia / FFmpeg / DSD 解码
│  ├─ seraph-dsp/         # 重采样与 DSD DSP
│  ├─ seraph-playlist/    # 播放列表与曲库模型
│  └─ seraph-visualizer/  # 频谱 FFT 与声学分析（K 加权响度/真峰/相关度）
├─ src-tauri/
│  └─ src/ipc/            # Tauri IPC、缓存、曲库、播放命令
└─ src/
   ├─ components/         # React UI
   ├─ hooks/              # 播放事件、拖放导入、波形等 hook
   └─ store/              # 前端 UI 状态与后端命令封装
```

## 播放状态机

当前播放队列、随机/循环模式、播放结束后的续播、`next_track` / `prev_track` 都由 Rust 后端处理。

前端负责：

- 同步当前队列快照到后端；
- 发送播放、暂停、上一首、下一首等命令；
- 监听后端 `TrackChanged`、`PlaybackStarted`、`PlaybackStopped`、`Progress` 等事件并更新 UI。

这样可以避免播放结束时前端自己推算下一首，导致 UI 状态和真实音频后端分叉。

## 缓存默认路径

缓存设置保存在应用数据目录的 `cache-settings.json`。已有设置文件的用户不会被自动迁移。

新用户首次启动时，默认缓存目录按以下优先级选择：

1. `<应用 exe 所在目录>/bilibili-cache`
2. `C:\ProgramData\Seraph Audio Player\bilibili-cache`
3. Tauri AppData 目录下的 `bilibili-cache`

每个候选路径都会尝试创建目录并写入 `.seraph-cache` 标记；如果不可写或不安全，会自动尝试下一个路径。

## 本地开发

### 环境要求

- Node.js 22+
- Rust stable
- Windows SDK / MSVC 工具链
- Tauri CLI 依赖由项目脚本调用

### 安装依赖

```bash
npm install
```

### 前端开发模式

```bash
npm run dev
```

### Tauri 桌面开发模式

```bash
npm run tauri:dev
```

### 类型检查与测试

```bash
npm run typecheck
npm test
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
npm audit --audit-level=low
```

这些检查也会在 GitHub Release 工作流中作为发布门禁执行。

### 版本升级

```bash
npm run bump 0.4.0
```

一次同步 `package.json`、`src-tauri/tauri.conf.json`、workspace `Cargo.toml` 三处版本号并刷新两个 lock 文件。

## 构建安装包

只生成 Windows EXE 安装器：

```bash
npm run tauri -- build --bundles nsis
```

同时生成 Windows EXE 安装器和 MSI：

```bash
npm run tauri -- build --bundles nsis,msi
```

产物默认位于：

```text
target/release/bundle/nsis/
target/release/bundle/msi/
```

当前 Tauri 配置会生成：

- NSIS EXE 安装器；
- `en-US` MSI；
- `zh-CN` MSI。

## 版本记录

### v0.5.8

**修复 v0.5.7 的任务栏歌词条回归 —— 使用 v0.5.7 的请务必升级。**

- **修复歌词条播控整排失效（v0.5.7 回归）**：v0.5.7 为 F-03 引入的「自定义 IPC 命令按窗口白名单」名单写漏了——歌词条实际调用 8 个命令，名单只放行了 3 个，导致**播放/暂停、上一首、下一首、进度条拖动、拖拽移动条**五项全部静默失效（`invoke` 被后端拒绝、前端 `.catch()` 吞掉，点了没反应）。根因是名单照着文档描述写、没核对前端真实代码。现补齐名单，并新增一条回归闸测试：**直接扫 `src/taskbar/` 源码解析出所有 `invoke(...)` 命令与白名单交叉核对**，漏放行或多放行都会让测试失败——这类「文档与代码脱节」的错不该靠人工比对兜底。已验证该测试确实能捕获此次故障。

安全方面落地第二份审查报告（`docs/SECURITY-REVIEW-2026-08-13.md`）的剩余发现：

- **SMTC 缩略图 URL 加白名单（N-02）**：系统媒体浮窗的封面是交给 **Windows Shell 进程**去取的，此前 `http://` 与任意 host 一律透传。曲库缓存是磁盘文件、可被离线篡改，等于借系统组件做任意出站请求（明文还可被中间人替换）。现要求 https + B 站官方图床域，不合规则不推封面（不影响曲目信息显示）。
- **图片域名白名单收敛为单一事实来源（N-03）**：此前头像、在线封面、SMTC 各写一份 host 判断，新增图片源就漏一处——N-02 正是这么漏的。现统一到 `ipc/url_guard`，并用「后缀带前导点 = 仅接受子域」的约定显式区分两类策略（B 站裸域也是有效图床入口，iTunes/QQ 的裸顶级域是官网而非图床，收窄不接受）。
- **M3U8 导出消毒换行与指令符（N-04）**：M3U8 是行敏感格式，曲目标签（外部输入）里的 CR/LF 会把一条记录劈成多条，可往导出的清单里注入伪造条目或 `#EXT` 指令，而清单常被分享给别的播放器读。现所有元数据字段的换行类字符归一为空格；路径含换行的条目直接跳过（路径不可能合法含换行）；`#` 开头的路径加 `./` 前缀消歧。
- **CSP `connect-src` 收窄（N-08）**：移除 `api.bilibili.com` / `music.163.com` / `lyrics.kugou.com` / `c.y.qq.com` 等七个第三方域。已核实前端零直连网络调用（无 `fetch`/`XHR`/`WebSocket`），全部 HTTP 都在 Rust 侧发起——这些域纯属冗余，留着反而给 XSS 提供现成的数据外发通道。现只保留 `'self'` 与 IPC。
- **Rust 依赖 unsound 公告清理（N-01）**：anyhow 1.0.102 → 1.0.104、event-listener 5.4.1 → 5.4.2，两条补丁版修复到位，CI 与发布门禁对应的 `--ignore` 已移除。目前仅剩 glib 一条（修复版需 0.20 而 tauri 2 上游锁 0.18，且属 Linux-only gtk 栈、Windows 产物不含）。
- **在线匹配的数据去向明示（N-09）**：在线歌词匹配弹窗补一行说明，告知会将曲名与艺术家发送至第三方搜索、不发送音频文件本身。

未落地项：Authenticode 代码签名（N-05 / S-03）需要代码签名证书；ffmpeg 定位链的签名校验（N-07）按报告建议不可行——上游 gyan.dev 的构建产物本身未签名，签名校验会对合法二进制一律失败，该链路的实际防线是下载时的 SHA-256 双源锚定（v0.5.6 S-01）。**`freezePrototype`（N-06）经实测不可采纳**：在冻结内置原型的等价环境下复跑生产 bundle，应用直接白屏，报错 `Cannot assign to read only property 'toString'`（依赖树中有库会写 `Object.prototype.toString`）。需要先定位并替换该依赖，否则启用即等于发一个打不开的版本。

工程化：Rust 测试 229 → 238（歌词条白名单与前端源码交叉核对、SMTC 非官方图床拒绝矩阵、图片域白名单前导点语义、M3U8 换行注入/含换行路径跳过/`#` 前缀消歧）。前端测试 149 不变。

### v0.5.7

独立安全复审专版——一次不参考既有报告、从零重审全仓的结果（`docs/SECURITY-REVIEW-INDEPENDENT.md`），逐项落地 1 严重 + 8 中危 + 低危加固：

- **修复单个音频文件可让整个进程 abort（严重 R-01）**：WAV 的 fmt 块把采样率写成裸 u32，symphonia 原样读取不做上界校验，该值一路流到 WASAPI 独占的输出配置，再乘声道数与缓冲秒数算出环形缓冲容量——一个 4 KB 的畸形文件即可让 `RingBuffer::new` 预分配约 192 GiB，分配失败走 `handle_alloc_error` 直接 abort 进程（`catch_unwind` 只兜 panic、兜不住 abort），主窗口与歌词条一起没。现在解码边界统一设闸：采样率钳在 8 kHz–768 kHz、声道 1–32、位深 ≤64，symphonia / ffprobe / DSD 三条路径共用同一道校验；ALAC magic cookie 的越界采样率改为忽略纠偏而非覆盖容器值（R-05）；DSD 头的采样率上界由 100 MHz 收到 49.152 MHz（按 PCM 768 kHz × 64 反推，仍覆盖 DSD1024）；环形缓冲与 WASAPI 队列各加一道容量硬上限作纵深防线（R-07）。
- **重定向逐跳复验域名（F-01/F-04/F-07）**：B 站音频下载、头像下载、在线封面此前只校验首个 URL，之后 reqwest 会跟随最多 10 跳——每一跳都是真实出站请求，等于盲 SSRF 探针（可探内网/localhost），且最终响应体会被当作可信内容落盘、音频还会直接交给解码器。现三条链路各自挂 `redirect::Policy::custom` 逐跳复验目标必须仍在白名单内，不合规直接判错（而非 `stop` 把 3xx 原样交回被误当正常响应）；iTunes 的 `artworkUrl100` 新增 https + 官方图床域白名单，封面图片下载与搜索接口分离为两个受限 client。
- **子进程与解码线程不再无限期挂死（R-03）**：`probe_with_ffprobe` 原为 `.output()` 无限等待，畸形文件或断连网络盘能把扫库线程永久钉住；现改为 spawn + 双管道并行排空 + 20 秒超时 kill。切歌/停止时 `worker.join()` 也是裸阻塞，解码线程卡在 ffmpeg 管道 read 上会把**引擎命令线程**一起拖死，此后所有播放命令永久返回 "audio thread is not available"、只能重启应用；现改为 5 秒有界 join，超时即 detach 保住引擎线程。
- **自定义 IPC 命令按窗口隔离（F-03）**：Tauri v2 的 capabilities 只约束 core/插件命令，`generate_handler!` 注册的 55 个自定义命令对主窗口和歌词条窗口一律开放——歌词条渲染的是外部歌词与元数据，其渲染面一旦被攻破即可调用任意文件读写与子进程命令。现在 `invoke_handler` 外层加中央白名单拦截：歌词条只放行它实际需要的四个只读命令（播放快照、曲目元数据、唤起主窗口、位置回传），未知 label 一律拒绝。
- **文件读写 IPC 加路径约束（F-02）**：`export_app_config` / `export_eq_preset` / `export_playlist_m3u8` 可写任意路径，`import_app_config` / `import_eq_preset` 连扩展名都不校验、任意 ≤2 MB 文件都能读出来回传渲染进程。现统一走 `path_guard`：写入要求绝对路径 + 扩展名白名单 + 父目录必须已存在 + 拒绝 `..` 点段；读取额外要求目标是已存在的普通文件。
- **歌词解析加行数与行长上限（W-01）**：后端此前只有 4 MB 总字节上限。数万行的 LRC 会让 LyricsPanel 一次性渲染同样多的 `<p>`（无虚拟化）；单行近 2 MB 时 TypewriterText 按 30 ms/字符逐字 `slice`（每次 O(n) 重建整串），跑完要 16 小时以上、期间每 30 ms 重建一次巨串，UI 永久冻结。现所有歌词来源在 `parse_lyrics_bytes` 一处收口：总行数 ≤5000、单行 ≤512 字符（按字符截断，不切碎中文），前端 TypewriterText 另有超长退化为静态显示的独立兜底。
- **子进程走系统绝对路径（F-05）**：`icacls` / `explorer` 此前按裸名启动，CreateProcess 搜索顺序包含应用自身所在目录——从下载目录或共享盘直接运行时存在同名 EXE 种植面；现统一解析到 `%SystemRoot%\System32\`。
- **其余加固**：同目录外部 `.lrc` 读取补 4 MB 上限，与 IPC 口径对齐（F-06）；ffmpeg zip 解压加 4096 条目数上限并在取齐目标后提前跳出（F-08）；DSD seek 的偏移乘法改 `saturating_mul`，消除 debug 构建下的溢出 panic（R-02）；歌单持久化状态改为元素级消毒，坏元素不再水合进 store 导致渲染期 `trackIds.includes` 抛 TypeError 白屏（W-03）；配置导入的 boot 写入包 try/catch，配额超限不再白屏启动（W-02）；GraphicEQ 解析改逐个 push，避免 5 万点预设触发展开实参上限 RangeError（W-06）；配置导入文本加 2 MB 上限（W-07）。
- **CI 与供应链（S-01/S-08/S-09/S-10）**：`npm ci` 全部加 `--ignore-scripts`（当前依赖树零安装脚本，零成本消除 PR 在 runner 上执行安装脚本的面）；CI 的 `npm audit` 阈值由 moderate 提到 low，与发布门禁同强度，消除「CI 绿、发布红」；发布流程新增 tag 与 `package.json`/`tauri.conf.json` 版本一致性校验，漏跑 bump 直接在门禁失败而不是发出版本号对不上的安装包；新增 dependabot 配置（actions / npm / cargo 每周），补上 SHA 锁定后拿不到上游修复的主动更新侧。

复审同时确认既有安全基线成立：FFmpeg 下载链的 SHA-256 双源锚定、URL 逐要素白名单、响应体全局上限、Cookie 只进 Credential Manager、生产 CSP 无 localhost 通配、asset scope 仅 covers 目录、`crates/` 全 workspace 零 `unsafe`、前端零 HTML sink，均经逐行复核；5 个按 commit SHA 锁定的 GitHub Action 已联网核验为对应仓库的真实提交。安装包代码签名（S-03）与仓库 tag protection（S-02）需要证书与仓库设置，不在代码变更范围内，留待后续。

工程化：Rust 测试 → 229（越界采样率的探测拒绝与正常文件不误伤、`validate_stream_info` 区间端点矩阵、环形缓冲容量封顶、歌词行数/行长上限与中文按字符截断、封面图床白名单正反矩阵、导出导入路径校验与 `..` 点段拒绝、歌词条命令白名单正反矩阵）。前端测试 147 → 149（歌单元素级消毒）。

### v0.5.6

安全加固专版——逐项落地 2026-08-05 安全审查（`docs/SECURITY-REVIEW-2026-08-05.md`）全部 15 项发现（1 高危 + 7 中危 + 7 低危）：

- **FFmpeg 下载完整性校验（高危 S-01）**：自动下载的 ffmpeg 压缩包此前解压后直接执行、无任何校验，上游被攻破即任意代码执行。现固定版本（9.0 essentials）双官方源（gyan.dev + 其官方 GitHub 镜像），硬编码官方 SHA-256（经双源实际下载交叉核验），下载后先验哈希再解压；解压单条目加 512 MB 硬上限——用 `Read::take` 在真实解压流上截断、不信任 zip 头声明的大小，防高压缩比条目写满磁盘（S-06）。
- **B 站登录态外泄面收窄（S-02/S-05）**：头像下载改用不带 Cookie 的裸 client，且 URL 必须是 https + 官方图床域名（hdslb.com/bilibili.com 系）白名单才发起请求——此前 API 返回的 `face` 字段未经校验就用携带 SESSDATA 的 client 直接请求；Set-Cookie 持久化加名称白名单（登录五件套 + buvid 系风控标识），名单外的响应 Cookie 不再进 Credential Manager。
- **外部响应一律限量读取（S-03）**：新增 `ipc/http_util` 公共防线，B 站 API、三源在线歌词、在线封面、更新检查的全部 JSON 响应改为 8 MB capped 增量读取（不信任 Content-Length），在线封面图片下载同步改为边读边限；此前任一上游异常返回超大响应都会被无界读进内存。
- **Release 页 URL 精确校验（S-04）**：检查更新的「前往下载」此前用 `starts_with` 前缀匹配，存在 path 拼接（`…/releases.evil.com/…`）与 `..` 点段穿越（含 `%2e` 编码变体，可跳到 github.com 任意仓库页）两类绕过；改为 URL 解析后 scheme/host/端口/userinfo 逐要素校验 + path 规范化后按段边界匹配。
- **本地边界加固（S-07/S-10/S-11/S-14/S-15）**：内嵌封面 32 MB 上限（选图与落盘双入口，防恶意音频撑爆内存/磁盘）；`CredReadW` 空 blob 判空（消除理论 UB）；session 临时文件先建空收 ACL 再写内容（消除默认权限暴露窗口）；默认缓存目录候选用户级 app_data 优先于多用户可写的 ProgramData；B 站短链跟随重定向后复验最终域名（防盲 SSRF 探测）。
- **配置与发布链收紧（S-09/S-12/S-13）**：生产 CSP 移除 `localhost`/`127.0.0.1` 通配（挪入 `devCsp` 仅开发期生效，渲染进程即使被 XSS 也无法探测本机端口）；歌词条窗口事件权限 `allow-emit` → `allow-emit-to` 定向主窗口，第二窗口不可再向任意窗口广播/伪造事件；GitHub Actions 全部按 commit SHA 锁定防 tag 漂移，CI 与发布门禁新增 `cargo audit`（Rust 依赖漏洞审计，unsound 类无修复版公告显式 ignore 并注明），CI 令牌显式只读。
- **依赖漏洞清零（S-08）**：`npm audit fix` 修复 undici（jsdom 测试链路）与 nanoid（审查报告之后新披露、位于生产依赖树 tailwindcss→postcss 下）两项高危公告，全量 `npm audit` 归零。
- **Rust 依赖漏洞清零（发布门禁补充）**：v0.5.6 打 tag 当天 `cargo audit` 在 CI 报出 quick-xml（RUSTSEC-2026-0194/0195）与 quinn-proto（RUSTSEC-2026-0185）三项新增高危公告，升级 plist 1.10.0（连带 quick-xml 0.41.0）与 quinn-proto 0.11.16 后归零，`cargo audit` 全绿。

工程化：后端测试 206 → 214（SHA-256 校验大小写不敏感与拒绝路径、zip 超限条目拒绝且半截输出清理、头像域名白名单正反矩阵、Cookie 名称白名单、Release URL 绕过用例矩阵、超限内嵌封面跳过与落盘拒绝）。前端测试 147 不变（S-12 为等价 API 收窄，typecheck 铆钉）。

### v0.5.5

任务栏歌词条打磨——三项针对性修复 + 一项设置增强：

- **修复导入歌词后歌词条不刷新**：歌词条按 trackId 缓存曲目元数据、只在切歌时重拉，给当前播放的曲目导入（或匹配在线）歌词后，条上会一直停在「暂无歌词稿」，要切走再切回才显示。现后端两条写歌词的路径写完曲库缓存即广播 `seraph://track-lyrics-updated`，条收到命中当前曲目就重拉一次。
- **修复呼出主窗口时歌词条跳向屏幕中部、切换应用后位置错乱**：歌词条用全局 `listen("tauri://move")` 接收自己的拖拽移动，而 Tauri v2 的 JS `listen()` 默认目标是 `Any`——`Any` 监听器无视 emit 目标全收，**主窗口**的窗口事件也一并进了歌词条的处理器。于是主窗口显示/移动时条会跳到主窗口的横坐标（居中窗口即屏幕中部），主窗口最小化时 Windows 把坐标改写成 -32000、条被钳到任务栏最左端，错误位置还会被持久化下来。现窗口事件一律按窗口 label 收窄监听。
- **修复切换应用时歌词条闪一下、消失又出现**：任务栏自身也是置顶窗口，Shell 在应用切换时重排置顶带 Z 序会把任务栏抬到条之上，此前只靠 500ms 心跳纠正，最长要半秒才恢复。跟随线程改为 Win32 消息泵线程并挂 `EVENT_SYSTEM_FOREGROUND` 钩子：前台切换的同一刻立即重申置顶，随后三拍（100ms 心跳）各补一次；避让检查与任务栏 rect 复查按节拍分摊，总开销与此前持平。
- **新增歌词条位置设置**：设置 → 系统集成新增带百分比读数的滑块，精确指定条沿任务栏的落位（0% 最靠左端、100% 最靠右端；任务栏竖排时对应上下端）。位置模型从「记忆像素坐标」改为「沿任务栏长边的比例」，换分辨率/DPI/任务栏方向都按比例还原；直接拖拽歌词条会反解成比例回传、滑块随之同步。位置的单一事实来源统一到主窗口设置（随配置导出/导入走），歌词条窗口不再自持久化，旧版本记忆的像素位置首次启动自动迁移。

工程化：后端测试 204 → 206（位置比例与像素坐标的双向映射、越界钳制、竖排任务栏沿长边落位）；前端测试 146 → 147（位置比例的持久化迁移与坏值兜底）。修复过程中另修掉一处改造引入的自锁：`if let Some(..) = *Mutex.lock()` 的守卫在 Rust 2021 下活到整个 `if let` 体结束，体内再取同一把非重入锁会当场死锁。

### v0.5.4

任务栏集成（新特性）+ 一项存量修复：

- **任务栏缩略图播控与图标进度**：悬停任务栏图标时缩略图下方提供上一首/播放暂停/下一首按钮（ITaskbarList3 ThumbBar，图标按任务栏深浅主题运行时光栅化、主题切换实时重建），任务栏图标叠加当前曲目播放进度（暂停转黄色）；设置 → 系统集成可分别开关，Explorer 重启自动自愈。
- **任务栏歌词条**：独立置顶浮层窗口贴靠任务栏，档案纸签样式——左缘印章红竖条 + 旋转封面 + 实时单行歌词 + 底边可点击进度线，悬停显示播控/回主窗/关闭按钮，可拖拽并记忆横向位置；跟随线程自动处理任务栏位置/DPI 变化重吸附、Shell 置顶带重排导致的遮盖、全屏应用与自动隐藏任务栏收起时的自动避让；任务栏深色主题时自动换墨签配色；支持鼠标穿透的「仅显示模式」。默认关闭；主窗口关闭时联动销毁。
- **修复部分曲目封面推不上系统媒体浮窗（SMTC）**：souvlaki 对 `file://` 封面地址剥前缀后按裸 Windows 路径打开，此前传规范百分号编码 URI 会成坏路径名（0x800700A1）；改按其约定传原生路径。

工程化：后端测试 176 → 204（任务栏渲染计划状态机、图标光栅化逐像素断言、歌词条几何/避让判定、souvlaki 封面契约）；前端测试 136 → 146（歌词行分组与定位提炼为共享模块、持久化迁移）。曾遇主线程死锁（同步命令建窗）、开关竞态（僵尸窗口）、切换应用条被任务栏盖住三坑，修复均有注释与测试铆钉。

### v0.5.3

单项针对性修复：

- **修复高解析度 ALAC（88.2kHz 以上）播放变慢降调、时长虚大**：MP4 stsd 音频条目的采样率字段是 16.16 定点数，整数部分只有 16 bit——96kHz 溢出截断成 30464（192kHz → 61568）。symphonia 的 isomp4 解封装把截断值填进解码参数，而 ALAC 解码器实际按 magic cookie 的权威采样率输出，导致输出流按错误采样率打开：96kHz 文件播放慢约 3.15 倍、音调严重降低，时长显示也虚大三倍多。现解码器打开 ALAC 时以 magic cookie 纠偏采样率、位深（stsd 不给 ALAC 填位深，此前默认按 16bit 打开输出，24bit 文件精度受损）与声道数。44.1k/48k 等常规采样率不受影响；曲库元数据显示路径（lofty）本就从 cookie 读采样率，无需改动。

工程化：后端测试 175 → 177（截断纠偏与 48 字节 cookie 偏移、非 ALAC/畸形 cookie 不误伤）。

### v0.5.2

单项针对性修复：

- **修复 WAV 封面显示不完全（只显示上半张/一条/空白）**：部分打标签工具在 WAV 内嵌 ID3v2.4 标签里把帧大小写成普通 u32 整数（规范要求 syncsafe 编码），封面帧超过 127 字节时两种解读不同，标签库 lofty 按规范解析导致 APIC 图片数据被拦腰截断——JPEG 渐进渲染即表现为半张图或空白（文字标签几乎都小于 128 字节，两种解读一致，不受影响）。新增 `wav_id3` 兜底模块：lofty 提取的封面缺失或缺 JPEG/PNG 结束标记（疑似截断）时，手动遍历 RIFF 定位 `id3 ` chunk（兼容其位于 RIFF 头声明大小之外的病例布局），以 mutagen 同款启发式消解帧大小的 syncsafe/u32 歧义（高位检测 → 无歧义短路 → 帧边界落点合法性判定）后重新提取完整封面。导入与启动补扫两条路径都挂兜底；封面补扫标记升级 v3——升级后首次启动自动重提取存量截断封面（持锁只取快照、重 IO 在锁外，回写校验并发导入安全），旧的半张图文件由孤儿封面清理回收。

工程化：后端测试 165 → 175（u32/syncsafe 歧义消解启发式、UTF-16 描述解析、文件级截断检测、越界 id3 chunk 提取与补扫全链路）。

### v0.5.1

三项针对性修复（含一项 v0.5.0 回归）：

- **修复独占模式周期性「嘀嘀」电流声（v0.5.0 回归）**：v0.5.0 把独占渲染循环改为按周期唤醒后，每拍全量填满可用空间——USB Audio 驱动按 1ms USB 帧粒度报告缓冲余量、瞬时值可能虚大，全满写入会覆盖设备正在读取的区域，在部分 USB DAC 上表现为周期性电流声。现改为：写入量按设备周期整数倍向下对齐、恒为缓冲保留一个周期的安全边界（绝不写到全满）、睡半个设备周期（2–10ms 钳制），并为设备缓冲深度加 100ms 下限（此前 16×period 在 352.8kHz 以上只有 ~23ms，睡眠抖动即可击穿）。v0.5.0 的可视化流畅度收益保留（tap 数据仍 ~10–20ms 一坨）。
- **修复 DSD（.dsf/.dff）曲目封面与标签解析**：标签库 lofty 不支持 DSD 容器，DSF/DFF 的内嵌 ID3v2（标题/艺术家/专辑/年份/封面）此前完全读不到——DSD 曲目一律「文件名当标题 + 无封面」。现按 DSF 规范（头部 metadata 指针指向文件尾 ID3v2 块）与 DFF 的 `ID3 ` chunk 手动定位标签字节块、交由 `id3` 库解析，封面经图片魔数校验落盘。启动封面补扫标记升级为 v2：已扫过的存量 DSD 曲目会自动再补扫一次拿到封面；标题/艺术家需重新导入文件刷新（导入合并会保留歌词与歌单归属）。
- **新增空格键播放/暂停**：焦点在播放器窗口内按空格即切换播放/暂停；检索框等文本输入与设置弹窗内的交互不受影响（正常打空格/操作按钮）；按键同时阻止页面滚动。

工程化：后端测试 163 → 165（DSF ID3 标签与封面提取全链路、测试专用 `parse_audio_metadata` 改为委托正式解析路径消除测不到真实链路的旧副本）；前端测试 131 → 136（空格键快捷键 5 测）；新增 `id3` 依赖。

### v0.5.0

本版本是一轮全代码库深度 BUG 审计（约 3.2 万行）后的集中修复与性能优化版：修复 1 项高危、19 项中危、10 余项低危问题，并重点优化了 WASAPI 独占模式下的可视化流畅度。

**独占模式可视化流畅度**（性能优化重点）：

- **渲染循环按设备周期补水**：独占模式渲染线程从「半个缓冲唤醒一次」（44.1/48kHz 下约 85–92ms）改为按设备周期（≈10ms，2–15ms 钳制）小口写入——频谱 tap 的数据节奏与共享模式事件回调一致，声学分析页与侧栏频谱在独占模式下不再退化成 ~12fps 的台阶动画；同时缓冲水位更满，underrun 风险更低。
- **tap 环形缓冲 16K → 64K 样本**：DoP / 352.8kHz+ 高采样率独占流下也能覆盖前端 30fps 轮询间隔，消除两次轮询之间溢出断流造成的频谱毛刺。
- **侧栏频谱平滑与重建修复**：柱体新增快攻慢放弹道（逐帧独立 FFT 结果不再硬跳变）；修复非整数 DPR（Windows 125%/150% 缩放）下浮点比较恒不等导致的每帧重建 canvas backing store；tap drain 移入分析枢纽锁内，侧栏与分析页并发轮询不再乱序喂 FFT。
- **分析页补齐断流衰减**：暂停后声场散点云随其他仪表一起优雅塌缩淡出（此前定格高亮）；自由布局编辑中贴右缘面板向右拖 resize 不再反向左移。

**高危修复——EQ 坏数据启动白屏死循环**：

- EQ 数据全链路消毒（新增 `eqSanitize` 模块）：JSON 预设导入、高级编辑输入框、persist 水合三个入口统一 isFinite / 钳制 / 结构校验，NaN 与字符串垃圾不再进入持久化；频响曲线 freq 补兜底。
- 新增根级 ErrorBoundary：界面渲染异常不再整页白屏，提供「重新加载」与「重置界面与音效设置」自救出口（保留曲库、歌单与喜欢标记；重置经 boot 阶段执行，绕开持久化 flush 竞态）。

**播放与引擎**：

- 修复队列自然播完后按媒体键「假恢复」——UI 显示播放中实际无声；`resume` 检查会话终止态，SMTC 自动落到重建会话路径。
- 切换输出设备失败时回滚到原设备并恢复播放，坏设备 id 不再残留导致后续播放持续失败。
- DSF seek 后时间戳按实际 block 对齐位置反推，进度显示与歌词对位不再有最高约 12ms 的恒定偏差；mono 输入的 LUFS 口径对齐 BS.1770-4（不再系统性 +3 LU）。

**M3U8 / 曲库链路**：

- 导入支持 GBK 编码 .m3u（千千静听、中文 foobar2000 等老播放器导出的中文路径不再乱码跳过）、`file:///` URI 条目（VLC 导出格式）；同一文件重复条目自动去重；超长清单截断显式提示。
- 导入/导出命令改异步执行（断连网络盘等慢 IO 不再冻结整个窗口）；导入歌单后留在当前页（不再被甩到本地音乐页）；路径映射归一分隔符（跨平台工具导出的清单不再静默丢曲）。
- 文件名数字前缀必须带分隔符才识别为曲号——「365天的思念」「24K Magic」不再被误剥成「天的思念」「K Magic」。

**歌词与在线服务**：

- 支持 `[mm:ss:cc]` 冒号百分秒 LRC 变体（千千静听时代常见格式，此前被按时:分:秒解析导致歌词导入成功但全程不滚动）。
- 在线歌词/封面区分「网络失败」与「未找到」，断网时不再谎报「没有匹配到」；三源改并发抓取（半死接口的超时不再逐源叠加）；本地时长未知时保持接口相关度排序（30 秒试听版不再排第一被自动选中）。

**Bilibili**：

- 收藏夹批量导入新增实时进度（N/total 与当前曲目）与「取消」按钮——大收藏夹导入不再是无法中断的黑盒；进度状态切页不丢。
- 修复 FLAC 与普通音质缓存路径冲突：先导普通流再开大会员导 FLAC 时，旧 AAC 文件不再被当 FLAC 命中（元数据张冠李戴且永不自愈）。
- 修复普通杜比 EAC3 5.1 被误判为杜比全景声（「优先杜比全景声」不再让有损流压过无损 FLAC）；ffmpeg remux 与登录会话落盘移出 async 上下文，批量导入期间其他命令响应不再变卡。

**前端 UI**：

- 修复搜索无结果一次后虚拟列表「失联」——此后所有列表页只渲染首屏十几行、下半屏空白滚动追赶。
- 设置弹窗按钮语义修正：所有设置均即时生效，收敛为单个「完成」按钮；缓存表单有未保存改动时自动提交（按钮变「保存并关闭」），不再静默丢失。
- 弹窗内选中文本拖到遮罩上松手不再误关弹窗丢输入；多弹窗同开时 Esc 只关最顶层；右键菜单打开期间列表变化后点「播放」不再播错歌；duration 未知的曲目 seek 不再被钳死为 0；APO 导出低/高通改写 LPQ/HPQ（保留自定义 Q）、增益 `+` 号前缀识别、44.1kHz 家族规格标签精度等十余项小修。

工程化：

- 前端测试 114 → 131（EQ 消毒模块 17 测）；后端测试 158 → 163（曲号分隔符判定、冒号百分秒变体、无时长排序保持、GBK 清单解码、file URI 与去重）。
- 完整审计报告（每条发现均含文件:行号、触发场景与修复建议，另附已验证干净的区域清单）已随 v0.5.6 从仓库移除（`docs/` 现以 `SECURITY-REVIEW-2026-08-05.md` 为现行审查档案）；上一轮（v0.3.6 时期）的 `docs/audit/` 旧报告与 `design/visualizer-demo/` 设计稿存档亦已从仓库移除。

### v0.4.9

新功能——**声学分析面板自由布局（LAYOUT 图纸编辑模式）**：

- **12×12 吸附网格**：分析页页眉新增「LAYOUT 布局」（≥1536px 宽屏开放），进入图纸编辑模式后六个仪表化为标注尺寸的虚线图框——拖标题行移动、拖右下角柄缩放，实时吸附到 12×12 视口分数网格（持久化值与设备无关，任何分辨率按比例呈现）。碰撞语义与专业仪表盘一致：被压面板递归下推、松手重力上浮压缩，推不开（会超出 12 行）的操作自动拒绝；各面板有最小跨度约束保证可读性。
- **编辑模式交互**：「完成 ✓」一次性提交（避免拖拽中反复刷持久化）、「取消」/ Esc 丢弃修改（拖拽中 Esc 先回弹当次操作）、「恢复默认布局」一键回到三区仪表台等价映射；窗口变窄自动退出编辑。编辑期间仪表渲染冻结，帧预算全部让给拖拽。
- **布局记忆与补位**：自定义布局持久化为 `seraph-analysis-settings` v2（`layoutMode` + `customLayout`），隐藏面板的位置始终保留——渲染时仅对可见子集重力压缩补位，重新显示自动回到记忆位置；窄屏（<1536px）纵向堆叠顺序按自定义布局的 (y, x) 阅读序推导。自动智能布局（v0.4.7 语义）保留为默认模式，不进编辑器完全无感。
- **随配置备份进出**：自定义布局随「配置备份」导出/导入；结构或数值非法的布局数据在迁移时整体判废回自动布局，轻微重叠由渲染路径自愈。
- **模式切换按钮精简为纯英文**：电平表 BAR / VU、瀑布 RIDGE / HEAT、声场 POLAR / LISSAJOUS（原中英混排）。

工程化：

- 新增 `gridLayout.ts` 纯函数布局引擎（碰撞检测 / 递归下推 / 重力压缩 / 边界与最小尺寸钳制 / 可见子集解算 / 窄屏排序 / 持久化结构校验，零 DOM 依赖）与 `AnalysisLayoutEditor` 图纸编辑组件（pointer 拖拽 + 4px 阈值防误触 + pointer capture + 取消回弹）。
- 前端测试 95 → 114：布局引擎 11 测（下推交换、溢出拒绝、脏数据自愈、隐藏补位、窄屏排序、结构校验）、设置 store v1→v2 迁移 5 测、编辑模式交互 3 测（进入 / 拖拽提交 / 取消不写入）、配置备份 v2 布局字段透传断言。
- headless Chrome CDP 实测：真实鼠标拖拽交换 / 角部缩放 / 取消回弹 / 窄屏顺序全部通过。

### v0.4.8

新功能——**声学分析补全四大监控域 + 全面自定义**：

- **示波器（NO.06）**：新增时间域波形仪表，至此四大核心监控域齐备（响度电平 / 频率域 / 相位声场 / 时间域）。后端把旁路样本喂入 2048 帧环形缓冲（~43ms 窗），i16 量化随分析帧下发；L 墨 / R 印章红双迹线，支持零交叉触发对齐（锁相稳定周期波形，默认开）与 L/R 分离显示，暂停后迹线优雅淡出。
- **电平表分行 + 模拟 VU 表**：条表模式改为 PEAK / RMS 每声道分行的水平条表（红区斜纹、峰值保持竖线保留，两类行可分别隐藏）；新增模拟 VU 表盘模式——L/R 双表盘、非线性 VU 弧刻度（-20～+3）、300ms 表针弹道（IEC 60268-17 语义）、0 VU = -18 dBFS（EBU R68）、超 0 VU 亮灯；BAR / VU 一键切换。
- **声学分析设置（PANELS 面板设置）**：分析页新增页眉与设置浮层，六个仪表可独立显示/隐藏，模块内内容项（目标偏差标尺、PEAK/RMS 行、相关度表、峰值保持虚线、触发对齐、L/R 分离）可单独开关；宽屏网格按可见面板集合自动重排补位（例如隐藏示波器即回到 v0.4.7 三区布局，全部隐藏有空态提示）。**全部选择持久化**——含声场极坐标/李萨如、瀑布山脊/热图、电平 BAR/VU、响度目标，重启保持。
- **配置一键导出 / 导入**：设置 → 系统集成新增「配置备份」——播放偏好（音量/随机循环/输出设备与驱动/SMTC/记忆播放）、EQ 与 DSP 全套、声学分析设置打包为单个 JSON 导出（保存对话框）；导入经结构校验后自动重载界面生效。收藏、歌单、最近播放、播放进度等个人数据不进导出文件，导入也不会覆盖本机数据。
- **频谱与瀑布显示自然化**：频谱主迹线 / 峰值保持线 / 山脊瀑布线全部改 Catmull-Rom 样条平滑（绘制区 clip 兜住陡变过冲），消除折线感；热图升采样启用高质量插值。

性能优化（声学分析页开销显著下降）：

- 渲染循环节流至 ~30fps（与数据帧率对齐；待机衰减期 ~14fps），不再全速 60fps 重画六块画布。
- 画布尺寸改 ResizeObserver 缓存，渲染循环零 `clientWidth` 读取，消除逐帧读写交错的强制 layout。
- 频谱瀑布改离屏缓存增量绘制：仅在走纸推进（~11fps）、模式或尺寸变化时重绘，其余帧只 blit；山脊模式 64 行 × 96 点的全量路径计算不再逐帧执行。

工程化：

- 后端测试 153 → 158（波形环形缓冲时序 / 越幅钳制 / 会话重置；配置文件读写与大小/BOM 防护）；前端测试 79 → 95（VU 弹道收敛、波形拆分与断流淡出、瀑布版本号、设置 store 持久化与坏值迁移、配置导出剥离个人数据 / 导入合并语义、面板隐藏与空态交互）。
- `AnalysisFrame` 新增 `waveform` 字段（交错 i16 量化）；新增 `export_app_config` / `import_app_config` IPC 命令与 `seraph-analysis-settings` persist store；配置导入经 sessionStorage 中转、在启动最早阶段（store 水合前）落盘，规避 player 持久化防抖在 pagehide flush 时覆盖导入数据的竞态。

### v0.4.7

**声学分析全屏模式**——点击「声学分析」后仪表撑满应用主区，减少 v0.4.6 双列布局的拥挤感：

- **右栏折叠**：进入分析页时右侧栏的小频谱 / 下一首 / 音频信息卡自动让位（与分析页仪表重复），仅保留一条窄歌词栏；退出分析页自动恢复完整右栏。
- **三区仪表网格**（≥1536px 视口）：左列响度计（自适应高度）+ 电平表（吃满剩余高度，长条表更接近专业表头）；右上频谱独占最大幅面；右下频谱瀑布（宽幅）+ 声场仪（近方形，极坐标不变形）。
- **响应式与缩放一致性**：大号响度读数改 `clamp()` 流式字号，随窗口宽度平滑缩放；画布按 devicePixelRatio 高清重采样；窄窗口（<1536px）回退为纵向滚动布局，并修复了此前面板高度可被压缩导致画布互相叠压的问题。已在 1366×768 / 1600×900 / 1920×1080 / 2560×1440（1.5x 缩放）下验证观感一致。
- 设计稿存档：`design/visualizer-demo/fullscreen.html`。

### v0.4.6

新功能——**声学分析套件**（侧栏「DRAWER C — 系统」新增「声学分析」入口，档案/打字机视觉语言）：

- **响度计（NO.01）**：K 加权响度按 ITU-R BS.1770-4 实现（高架 + RLB 高通两级 biquad、任意采样率参数化设计），瞬时 M（400ms）/ 短期 S（3s）/ 整体 I 三窗读数；整体响度采用绝对 -70 LUFS + 相对 -10 LU 门限积分；LRA 按 EBU Tech 3342（短期分布 P95−P10，含 -20 LU 相对门）；真峰为 Catmull-Rom 4x 内插近似（标注 ≈）；目标制偏差标尺（-14 流媒体 / -16 播客 / -23 EBU 广播 / -9 母带参考），超 -1 dBTP 盖「OVER」印章。
- **电平表（NO.02）**：双层条（实墨 RMS + 浅棕峰值）、1.4s 峰值保持横杠（红区转印章红）、0～-6 dBFS 红区斜阴影线、「CLIP」印章 3s 自动解除。
- **声场仪（NO.03）**：goniometer 散点（M/S 变换极坐标，单声道收顶部竖线、宽立体声铺扇面）+ 6 帧墨点余晖，可切李萨如模式；相关度表 ρ∈[-1,+1]，负相关印章红警示。
- **频谱（NO.04）**：96 频点（FFT 4096，log 分箱）、峰值保持虚线（3.5 dB/s 缓落）、悬停游标在面板头显示频率/dB 读数，迹线语言与 EQ 页频响曲线同源。
- **频谱瀑布（NO.05）**：64 帧 × 96 频点环形缓存；「山脊」模式为墨线逐帧纵深堆叠（高能量段描红）、「热图」模式为纸→浅褐→棕→印章红→墨的墨渍色阶。
- **数据管线**：复用渲染线程 SpectrumTap 旁路（新增采样率元数据），侧栏频谱与分析页经同一分析枢纽 drain（互不抢样本）；换曲目自动重置积分/LRA/真峰会话值；暂停后仪表优雅衰减、瀑布继续走纸；纯浏览器开发模式回退内置模拟信号源。

工程化：

- 后端测试 145 → 153（EBU -23 LUFS 参考正弦校准、门限积分抗静音、真峰采样间过冲、相关度 ±1、LRA 20dB 双段、会话重置、tap 元数据）；前端测试 70 → 79（弹道学状态机、面板渲染与模式切换）。
- `seraph-visualizer` 新增 `analysis` 模块；新增 `get_analysis_frame` / `reset_analysis_meters` IPC 命令；`design/visualizer-demo/` 保留五面板独立 HTML 设计稿。

### v0.4.5

- **默认输出改为系统共享**：新用户默认音频输出从 WASAPI 独占改为系统共享（兼容性优先），已有用户保留自己的选择。
- **双击播放**：曲目行双击即强制起播（停止态也生效），单击保持"仅选中"语义。
- **B 站流媒体「重新加载…」**：流媒体曲目右键新增重新加载，弹窗可选杜比全景声 / FLAC 无损 / FFmpeg 混流后按所选音质重新解析下载，原位替换曲库记录（保留收藏与歌单归属）；流媒体导入偏好默认全部不勾选。

### v0.4.4

新功能——**EQ 均衡器与 DSP 链**（侧栏「DRAWER C — 系统」新增入口）：

- **参数均衡器**：RBJ biquad（Audio EQ Cookbook）级联实现，默认 10 段（31Hz–16kHz，首尾搁架 + 中间峰化），支持逐段编辑频率/增益/Q/滤波类型（峰化 PK / 低架 LSC / 高架 HSC / 低通 LP / 高通 HP）、添加/删除频段（最多 40 段）、预放大（±24dB）；实时频响曲线预览（20Hz–20kHz 对数轴）。
- **Crossfeed（耳机串扰馈送）**：对侧信号经一阶低通后按强度混入，缓解耳机硬声像分离的听感疲劳；强度与截止频率可调。
- **曲风预设**：内置平直/摇滚/流行/爵士/古典/电子/嘻哈/人声/低音增强/高音增强 10 种预设（带防削波 preamp）。
- **AutoEq / EqualizerAPO 导入**：直接导入 AutoEq 的 `ParametricEQ.txt`（Filter 行）与 `GraphicEQ.txt`（点列自动转参数段）；识别 PK/LSC/HSC/LP/HP 类型与 Preamp 行。
- **用户预设与导入导出**：当前配置可命名保存为本地预设；支持导出为 JSON（完整配置）或 EqualizerAPO txt（可回导 APO/其它播放器）。
- **DSD 生效开关**：EQ 页内提供「EQ 对 DSD 生效」开关，默认关闭（DSD 解码为 PCM 后默认直通保持「原汁」），开启后 DSP 链同样作用于 DSD 曲目。
- **引擎集成**：DSP 链运行在解码线程（重采样后、写入环形缓冲前），实时回调零改动；配置经版本号热更新（拖动 slider 播放中即时生效、滤波状态跨包保留）；seek 时与重采样器一同重置滤波状态；EOF flush 样本同样过链，gapless 接缝能量连续；链禁用/平直时 bit-exact 零成本直通。

工程化：

- 后端测试 124 → 145（EQ 系数/稳定性/直通、crossfeed、DSP 链热更新与 serde、预设文件 IPC）；前端测试 49 → 63（APO/AutoEq 解析器、EQ store）。
- `seraph-dsp` 新增 `eq` / `crossfeed` / `chain` 模块；`seraph-decoder` 导出 `is_dsd_file`；新增 `set_dsp_settings` / `import_eq_preset` / `export_eq_preset` IPC 命令。

### v0.4.3

新功能——**全局右键菜单**（自绘纸质档案风格，屏蔽 WebView2 浏览器默认菜单）：

- **曲目行**（本地音乐/最近播放/我喜欢/专辑详情/艺术家详情）：播放·暂停·重新缓存并播放（按状态切换）、收藏、加入歌单（子菜单直选，已加入置灰，可新建歌单并加入）、曲目信息、复制（标题—艺术家 / 文件路径）、打开文件所在位置、删除曲库记录（仍走确认弹窗）。
- **歌单详情页曲目行**：在通用菜单基础上追加上移/下移/移出本歌单。
- **歌单卡片**：打开、重命名（新入口）、导出 M3U8、删除歌单。
- **专辑/艺术家卡片**：打开、从第一首开始播放、整组加入歌单。
- **播放条当前曲目**：收藏、加入歌单、在线匹配封面、在线匹配歌词（联动歌词面板搜索弹窗）、曲目信息、打开文件所在位置。
- **UP NEXT 卡片**：立即播放这首、收藏、加入歌单、曲目信息；「更多」按钮左键弹出同款菜单。
- **歌词稿**：复制这句/复制整篇歌词、在线匹配、导入本地歌词。
- **曲目信息弹窗**：完整展示格式/规格/采样率/码率/声道/文件大小/时长/歌词行数/来源/文件路径，支持复制路径与定位文件。
- **交互细节**：菜单贴视口边缘自动翻转、子菜单方向自适应、外点/Esc/滚动/失焦关闭；文本输入框保留系统复制/粘贴菜单；「打开文件所在位置」由新增的 `reveal_in_explorer` 命令实现（阻塞校验移出主线程，路径存在性后端兜底）。

工程化：

- 前端测试 38 → 49（右键菜单 store、歌单重命名/创建返回 id、曲目行右键与全局删除确认）；后端测试 56 → 59（reveal 路径校验）。

### v0.4.2

新功能：

- **记忆播放开关**：设置 → 系统集成 新增「记忆播放」开关（默认启用）。开启时重启后恢复上次曲目与播放位置；关闭后每次启动从头开始，且不再在本地记录上次播放进度。设置持久化，重启保持。

修复（一轮系统性 BUG 筛查后的集中修复）：

- **修复主线程冻结族（高）**：曲库/缓存写命令（删除曲目、保存/应用歌词、清理缓存、更新缓存设置）与播放控制命令（播放/暂停/停止/定位/上下一首/切驱动/切设备）全部改为异步执行，阻塞 IO 与引擎等待移出主线程；旧曲库首启的封面补扫收窄持锁范围（重标签解析在锁外完成），不再与写命令抢锁导致窗口分钟级白屏。引擎命令回执增加超时兜底，引擎异常挂起时返回错误而非永久阻塞。
- **修复音量恢复错误（高）**：拖动音量条到 0 时以拖动前的真实响度记录恢复基准，取消静音不再只恢复到 1–3%。
- **收敛 B 站下载凭据暴露面（中）**：下载音频前强制校验为 https + B 站系 CDN 域名白名单，登录 Cookie 不再随 playurl 响应里的第三方/明文地址外发。
- **修复重导入静默丢失封面（中）**：重新导入已在线匹配封面的曲目时保留既有封面引用，不再被孤儿封面清理误删。
- **修复缓存保护防线退化（中）**：缓存清理/配额执行时先按磁盘真实状态校验 `.seraph-cache` 标记再创建目录，恢复「防止误删非缓存目录」的保护；修正默认缓存目录兜底路径绕过安全校验的问题。
- **修复 FFmpeg 下载卡死（中）**：下载客户端增加读取空闲超时，网络静默停滞不再永久挂起并占用下载槽位；前端在下载落定后忽略滞后到达的进度事件，避免按钮永久转圈。
- **频谱与重采样精度（中/低）**：修正频谱归一化未补偿窗函数相干增益导致的全频段约 6 dB 偏低；有状态重采样器预填充滤波上下文，消除每个播放会话开头丢弃约 16 帧、gapless 接缝处的可闻跳变。
- **其他**：随机播放下 UP NEXT 卡片改为「所见即所得」（点击即播卡片上显示的那首）；切换输出驱动时命令串行化；收窄 seek 时的解码锁作用域；去重解码错误与设备丢失并发时的重复停止事件；频谱面板暂停归零后停止渲染循环，不再空转。

工程化：

- 后端测试 54 → 56（B 站下载 URL 白名单）、频谱校准测试补充；前端测试 36 → 38（记忆播放开关持久化与迁移）。

### v0.4.1

- **SMTC 持久化开关**：设置页新增「系统媒体控件」开关（默认启用）。停用后系统浮窗/锁屏不再展示播放内容、媒体键失效；重新启用立即恢复并同步当前曲目显示。设置持久化，重启保持。
- **设置页分类分页**：设置窗口按功能拆分为四个标签页——音频输出（驱动/设备/输出能力）、缓存管理、系统集成（SMTC）、关于与更新，便于查找。
- 修复 M3U8 单元测试并行执行时临时目录互删的竞态（仅影响 CI，不影响功能）。

### v0.4.0

架构与存储：

- **模块结构重构**：`library` 与 `bilibili` 从 `include!` 拼接改为真模块树（共享项收敛到各自 `prelude`，跨模块项显式 `pub(crate)`），恢复模块边界与 rust-analyzer 索引能力。
- **曲库存储改造**：曲库快照内存常驻（读命令零磁盘 IO），歌词从主 JSON 拆分为 `library-lyrics.json` 边车文件（旧格式自动迁移），主文件写入体积与序列化成本显著下降；保持原子写、损坏备份拒写、读改写互斥等既有保护。
- **结构化 IPC 错误**：library/cache 命令族迁移到 `IpcError`（thiserror，序列化为 `{ code, message }`），前端可按错误码分支处理。

新功能：

- **SMTC 系统媒体控件**：键盘/蓝牙媒体键播放、暂停、切歌、定位；系统音量浮窗与锁屏展示标题/艺术家/专辑/封面/进度。媒体键动作与应用内操作走同一控制路径。
- **实时频谱可视化**：渲染循环以实时安全方式（try_lock + 预分配环形缓冲，绝不阻塞音频线程）旁路输出样本，FFT 在 IPC 线程计算，右栏 48 段 log 频谱 ~30fps 展示，暂停自动衰减收敛。
- **曲库检索与排序**：所有曲目列表页支持标题/艺术家/专辑检索（大小写不敏感）与默认/标题/艺术家/专辑/时长排序，过滤只影响展示不影响播放队列。
- **播放进度恢复**：按 5 秒粒度持久化播放位置，重启后恢复上次曲目与进度，播放键直接续播。
- **应用内检查更新**：设置页手动检查 + 启动静默检查 GitHub Releases，发现新版一键打开下载页（URL 白名单校验）。
- **歌单增强**：曲目行「加入歌单」入口 + 选择弹窗；歌单详情页支持排序（上移/下移）、移除、导出 M3U8；支持从 M3U8 清单导入并自动建歌单。
- **封面覆盖面扩展**：UP NEXT 卡片与艺术家页展示封面；无内嵌封面的本地曲目可在线匹配（QQ 音乐优先、iTunes 兜底，内容哈希落盘）；covers 目录孤儿封面启动自动清理（1 小时宽限期防误删）。

工程化：

- 前端测试 13 → 36 项（引入 @testing-library/react：检索/排序/歌单弹窗等组件交互覆盖）；后端测试 41 → 54 项（M3U8 解析、频谱环形缓冲、版本比较、封面 GC 等）。
- 新增 `npm run bump x.y.z` 版本同步脚本（三处版本声明 + 两个 lock 文件一次到位）；修正 workspace `repository` 字段。

### v0.3.8

- **本地封面识别**：导入本地曲目时用 lofty 提取内嵌封面（优先 Front Cover；MIME 缺失时按魔数识别 JPEG/PNG/GIF/BMP/WEBP），按内容哈希写入应用数据目录 `covers/`（同专辑多曲共用一张图，曲库 JSON 只存路径）；旧曲库首次启动自动一次性补扫（后台线程执行，完成后写标记，后续启动零开销）；封面经 Tauri asset 协议加载，访问范围仅开放 `covers` 目录。
- **播放条转盘封面化**：当前曲目有封面时（本地提取或 B 站视频封面），封面以圆形填入左下角转盘并沿用原旋转动画；无封面或封面加载失败时回退默认同心圆刻度盘。
- **专辑视图封面**：专辑网格展示本地提取的封面；专辑内第一首缺图时用组内其他曲目封面兜底。
- **界面减拥挤改版**：页面大标题头压缩为单行（描述超宽截断、悬停显示全文）；流媒体页控制台去掉三段分区标签，压缩为「归档输入行 + 账号/音质开关行」两行工具条；播放队列改为占满剩余高度，1080p 下可见行数明显增加（本地音乐、最近播放、我喜欢、专辑/艺术家详情页同步生效）。
- **修复**：B 站登录/FFmpeg 状态刷新在 `invoke` 返回 `undefined` 时不再把状态覆盖为 `undefined`（此前会导致流媒体页读取 `loggedIn` 崩溃）。

### v0.3.7

- 第二轮全代码库 BUG 审查（四通道并行 + 对 v0.3.6 修复提交的回归审查），修复 2 项高危、11 项中危及 20 余项低危问题。
- 音频引擎：修复暂停→恢复后左右声道持续互换（渲染消费改为严格按帧对齐，v0.3.6 增益斜坡引入的回归）；stop/切歌路径补全 ramp-out，独占模式退出前等待设备缓冲排空，消除硬切爆音；seek 失败时回滚进度并提示，不再与实际声音持续错位；探测畸形文件的 open/probe 路径加 panic 隔离，坏文件不再导致所有播放功能永久失效；4.0/5.0 声道布局下混不再误丢声道；曲终收尾与用户停止并发时不再误触发自动切歌；消除 seek 竞态下短暂闪回旧位置音频的窗口；开播 1 秒内重复点播同曲现在正确从头播放。
- 解码器：Symphonia seek 裁剪按 time_base 正确换算为帧（修复 MP4/MKV 类容器 seek 落点偏差）；ffmpeg 崩溃检测改为轮询等待退出码，中途崩溃不再被误判为正常播完；DSF/DFF 声明时长与文件真实大小交叉校验，截断文件不再显示虚大时长。
- Tauri 后端：B 站会话文件改为原子写入 + 损坏自愈（此前损坏会导致全部 B 站功能瘫痪且需手动删文件）；缓存清理移出 async 直调避免阻塞运行时；收藏夹批量导入不再逐首触发缓存清理误删同批文件；FFmpeg 下载加并发保护；KRC 翻译歌词对齐修复；remux 失败后的 fallback 缓存可被复用不再重复下载。
- 前端：修复启动水合空窗期内操作会用默认状态覆盖 localStorage、导致收藏/歌单不可逆丢失的高危问题；播放代际守卫下沉到 play 命令发出前，快速切歌不再出现"实际播 A、显示 B"；B 站重缓存加进行中去重，重复点击不再触发重复下载；重缓存失败后播放状态正确复位；流媒体页的 FFmpeg 下载与扫码登录状态提升到全局 store，切页不再中断登录轮询或重置下载进度；时长格式化支持小时段并防 NaN；音量拖到 0 后取消静音恢复正确音量等多项修复。

### v0.3.6

- 完成全代码库深度审查（约 1.8 万行），审查报告见 `docs/audit/`，共修复 60+ 项问题。
- 音频引擎：新增每样本增益斜坡，消除暂停/恢复、seek、音量调整时的爆音与 zipper noise；seek 越界钳制；解码线程 panic 兜底；WASAPI 初始化超时不再阻塞引擎线程；5.1/7.1 → 立体声按 ITU 系数下混；16bit 输出加入 TPDF dither 并统一四舍五入量化。
- 解码/DSP：修复 DSF 末尾 padding 导致的曲目结尾爆音；DC blocker 截止频率按实际 PCM 采样率计算；DSD 容器头严格校验（防损坏文件导致崩溃或死循环）；DSD 回放 +6 dB 增益补偿；启用 Symphonia gapless；seek 精确到样本级；修正 DoP 打包字节序。
- Tauri 后端：修复 B 站登录 Cookie 可能泄漏到任意链接的安全问题（host 白名单）；曲库缓存改为原子写入并对损坏文件备份拒写；下载改用专用超时策略客户端；`.eac3` 纳入缓存配额管理；曲库读写加锁防并发丢更新；移除 ffmpeg 第三方下载镜像。
- 前端：启动后恢复上次播放曲目；快速切歌请求代际守卫；歌词应用锁定目标曲目；修复两处事件监听器泄漏；进度条拖动后抑制旧进度回跳；虚拟列表随窗口尺寸自适应。

### v0.3.5

- 修复歌曲列表序号在不同页面中仍显示全局固定编号的问题。
- 当前页面、筛选结果和虚拟滚动列表会按视图内顺序重新显示 `REC.001`、`REC.002` 等编号。
- 播放逻辑仍使用全局播放队列索引，不影响点击播放、收藏和删除操作。

## GitHub Release

仓库包含 `.github/workflows/release.yml`。推送 `v*` tag 时会触发 Windows release 构建，并发布安装包到 GitHub Release。

示例：

```bash
git tag v0.5.5
git push origin v0.5.5
```

## 许可证

MIT License
